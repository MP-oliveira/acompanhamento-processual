# Variáveis de Ambiente para Produção - Vercel

# Configurações do Servidor
NODE_ENV=production
PORT=3001

# Configurações do Banco de Dados (Substituir pela URL real do Supabase)
DATABASE_URL=postgresql://postgres:senha@localhost:5432/acompanhamento_processual

# Configurações JWT (Substituir por uma chave secreta real)
JWT_SECRET=jwt_secret_muito_seguro_para_producao_123456789
JWT_EXPIRES_IN=24h

# Configurações da API Externa (DataJud) - COMENTADO PARA USO FUTURO
# DATAJUD_BASE=https://api-publica.datajud.cnj.jus.br/api_publica_tjba/_search
# DATAJUD_TOKEN=token_datajud_real_aqui

# Configurações de Log
LOG_LEVEL=info

# Configurações do Supabase (se usando)
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_ANON_KEY=sua_chave_anonima_supabase

# Configurações de Email (se configurado)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu_email@gmail.com
SMTP_PASS=sua_senha_app_gmail

# Configurações VAPID para Push Notifications
VAPID_PUBLIC_KEY=sua_chave_publica_vapid
VAPID_PRIVATE_KEY=sua_chave_privada_vapid
VAPID_EMAIL=mailto:seu_email@exemplo.com
